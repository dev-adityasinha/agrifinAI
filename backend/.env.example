# Backend Environment Variables (Express.js)

# Server Configuration
PORT=8080
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# MongoDB Configuration (Local Development)
MONGODB_URI=mongodb://localhost:27017/agrifinai
# Production (MongoDB Atlas): mongodb+srv://username:password@cluster.mongodb.net/agrifin?retryWrites=true&w=majority

# JWT Secret (IMPORTANT: Use strong random secret in production!)
# Generate: https://generate-secret.vercel.app/32
JWT_SECRET=e48287ee414c33752df93edf85ab1594
JWT_EXPIRES_IN=7d

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-email-password